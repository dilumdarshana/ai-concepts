services:
  neo4j:
    image: neo4j:5.26
    container_name: rag-graph-neo4j
    ports:
      - '7474:7474'
      - '7687:7687'
    environment:
      - NEO4J_AUTH=neo4j/password
      - NEO4J_PLUGINS=["apoc"]
    volumes:
      - ./neo4j-data:/data
    restart: unless-stopped
